[build]
builder = "nixpacks"

[deploy]
startCommand = "export TZ=Asia/Tokyo && cd backend && python3 -m uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
